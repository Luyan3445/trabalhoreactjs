import React from 'react';
import './Footer.css';

const Rodape: React.FC = () => {
  return (
    <footer className="rodape">
      <small>Sei la, vivendo</small>
    </footer>
  );
};

export default Rodape;

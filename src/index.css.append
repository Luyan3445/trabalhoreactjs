/* root global styles required */
:root{
  --bg:#f8f9fa;
  --text:#222;
  --accent:#6b46c1;
  --card:#fff;
}
body{background:var(--bg);color:var(--text);font-family:Inter,Arial,Helvetica,sans-serif;margin:0;}
.app main{padding:1rem;}
.header .link-like{background:none;border:0;color:inherit;cursor:pointer;margin-left:10px;text-decoration:underline;}
